// Lindek-list
function LindekList() {
    let head = null
    let length = 0

    const add = (value) => {
        if (head) {

        }
    }


    return {

        length: () => length
    }
}
const list = LindekList()

console.log(list.length())